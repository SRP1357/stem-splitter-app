# model-chunks

Runtime ONNX models split into <100 MB parts so the website can fetch them via raw.githubusercontent.com (which serves CORS headers, unlike GitHub Release downloads). Regenerated by scripts/publish-model-chunks.mjs on main. See main branch README.
